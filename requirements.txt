# xx_school_rag 依赖清单（版本为实测通过的组合，Python 3.10）
# 安装： pip install -r requirements.txt
#
# 注意：
#   - torch 默认装 CPU 版；有 GPU 按官网命令单独装对应 CUDA 版本。
#   - pymilvus 3.0.1 与 Milvus 2.4.0 服务端实测兼容；换服务端版本需自行验证。
#   - models/ 下的本地模型不在 pip 依赖里，见 models/README.md。

# ---- 基础 ----
python-dotenv==1.2.3

# ---- 数据层 ----
PyMySQL==1.2.0
redis==8.1.0

# ---- BM25 快速通道 ----
jieba==0.42.1
rank-bm25==0.2.2
numpy==2.2.6

# ---- 向量检索 (Milvus + BGE-M3 + Reranker) ----
pymilvus==3.0.1
milvus-model==0.2.12
FlagEmbedding==1.4.2
datasets==5.0.1
peft==0.20.0
scipy==1.15.3
onnxruntime==1.23.2
torch==2.13.0
transformers==5.16.1
sentence-transformers==6.0.1

# ---- 意图分类器训练/推理 ----
scikit-learn==1.7.2

# ---- 文档加载 / 切分 ----
langchain==1.3.17
langchain-core==1.6.0
langchain-community==0.4.2
langchain-text-splitters==1.1.2
pymupdf==1.28.2
opencv-python==5.0.0.93
python-docx==1.2.0
pillow==12.3.0
tqdm==4.70.0
pandas==2.3.3

# ---- LLM (DeepSeek, 兼容 OpenAI 接口) ----
openai==3.3.1
langchain-openai==1.6.0

# ---- Web 服务 (仅 app.py 需要，命令行 main.py 不需要) ----
fastapi==0.141.1
uvicorn==0.52.4
websockets==16.1.1

# ---- RAG 效果评估 (仅 scripts/eval_ragas.py 需要) ----
#   ragas==0.4.3 在 import 时会无条件加载 langchain_community.chat_models.vertexai，
#   但本仓库锁定的 langchain-community==0.4.2 已经把这个子模块整个移除了，
#   eval_ragas.py 里用一个占位模块把这次 import 垫过去，不需要改动这两个包的版本。
ragas==0.4.3

# ---- 可选：生成自定义 pdf 文档时用到 ----
reportlab==5.0.1

# ---- 可选：加载 .ppt/.pptx 时需要 ----
# python-pptx

# ---- 可选：pdf/docx/图片里含图片需 OCR 时需要 ----
# rapidocr-onnxruntime
